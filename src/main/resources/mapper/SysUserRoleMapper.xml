<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.managersystem.mapper.SysUserRoleMapper">

    <resultMap type="com.example.managersystem.domain.SysUserRole" id="SysUserRoleMap">
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="roleId" column="role_id" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询所有-->
    <select id="queryAll" resultMap="SysUserRoleMap">
        select user_id, role_id
        from sys_user_role
    </select>

    <!--查询单个-->
    <select id="queryById" resultMap="SysUserRoleMap">
        select user_id, role_id
        from sys_user_role
        where user_id = #{userId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SysUserRoleMap">
        select
        user_id, role_id
        from sys_user_role
        <where>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="roleId != null and roleId != ''">
                and role_id = #{roleId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sys_user_role
        <where>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="roleId != null and roleId != ''">
                and role_id = #{roleId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" useGeneratedKeys="true">
        insert into sys_user_role(user_id, role_id)
        values (#{entity.userId}, #{entity.roleId})
    </insert>

    <insert id="insertBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into sys_user_role(user_id, role_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.roleId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into sys_user_role(user_id, role_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.roleId})
        </foreach>
        on duplicate key update
        role_id = values(user_id), zipcode = values(role_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sys_user_role
        <set>
            <if test="userId != null and userId != ''">
                user_id = #{userId},
            </if>
            <if test="roleId != null and roleId != ''">
                role_id = #{roleId},
            </if>
        </set>
        where user_id = #{userId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from sys_user_role
        where user_id = #{userId}
    </delete>

</mapper>
